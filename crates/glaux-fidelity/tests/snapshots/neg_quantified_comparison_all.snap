# glaux fidelity snapshot — generated by glaux-fidelity; do not edit by hand
# case: neg_quantified_comparison_all
# provenance: UNVERIFIED recorded=2026-08-22 — recorded from glaux itself because no AWS profile was available; re-record with `cargo run -p glaux-fidelity -- record`
# compare: unordered
status: FAILED
error: "NOT_SUPPORTED: quantified comparison (ALL) is not supported: `x > ALL (...)` is valid Trino, but glaux does not translate the quantified comparison predicates (ALL / ANY / SOME) in v0.1: DataFusion has no equivalent, and its own rewrite of the shapes it does parse turns them into `cardinality` / `array_max` / `array_min` calls whose NULL rules are not Trino's. Rewrite the predicate: `x = ALL (s)` / `x <> ALL (s)` as `x NOT IN (s)` or a NOT EXISTS, and the ordering forms as a comparison against `(SELECT max(y) FROM s)` / `(SELECT min(y) FROM s)`, handling an empty or NULL-bearing s yourself"
